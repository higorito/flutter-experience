import 'package:flutter/material.dart';

class DocumentosScanConfirmPage extends StatelessWidget {
  const DocumentosScanConfirmPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
