import 'package:flutter/material.dart';

class Etapa4DocumentosPage extends StatelessWidget {
  const Etapa4DocumentosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
