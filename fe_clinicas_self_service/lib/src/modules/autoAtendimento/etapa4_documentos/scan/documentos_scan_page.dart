import 'package:flutter/material.dart';

class DocumentosScanPage extends StatelessWidget {
  const DocumentosScanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
